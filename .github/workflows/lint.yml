name: Lint

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  # frontend-lint:
  #   name: Frontend Linting
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Check out Git repository
  #       uses: actions/checkout@v4

  #     - name: Set up Node.js
  #       uses: actions/setup-node@v1
  #       with:
  #         node-version: 20

  #     - name: Install Frontend dependencies
  #       working-directory: ./FrontEnd  # Adjust this path to your frontend directory
  #       run: npm ci

  #     - name: Run frontend linters
  #       uses: wearerequired/lint-action@v2
  #       with:
  #         eslint: true
  #         prettier: true
  #         eslint_dir: FrontEnd  # Specify frontend directory for ESLint
  #         prettier_dir: FrontEnd  # Specify frontend directory for Prettier

  backend-lint:
    runs-on: ubuntu-latest
    name: Lint and Format Backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "20"

      - name: Install backend dependencies
        working-directory: ./BackEnd
        run: npm install

      - name: Run ESLint and Prettier for backend
        working-directory: ./BackEnd
        run: npm run lint:fix